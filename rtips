# R tips

# 1
# directorio de trabajo o dónde está la carpeta**
getwd()

# 2
# para directorios con 1 barra \ que dan error
# prueba \\ en vez de \ 
# o prueba / en vez de \
# porque / es para todos los sistemas operativos
# y \\ es para Windows

# 3
# establecer directorio
setwd("E:/Curso/tips/")
# chequeo
getwd()

# 4
# abrir RStudio desde carpeta de archivo
# permite omitir la ruta del directorio

#
# ayuda sobre comando
# ? + comando
?par

#
# ayuda sobre tema
# ?? + palabra
??color

#
# leer archivo .txt
# txt (separado por espacios)
nombre <- read.table("nombre.txt",dec=",",header=T)

# REVISAR
# leer archivo .csv
# csv separado por comas
datos <- read.csv("datos.csv",header=T)
# csv separado por ;
datos <- read.csv2("datos.csv",header=T)
# leer archivo .xls
library(readxl)
datos <- read_excel("datos.xls",col_names=T)
# 

# REVISAR
# exportar
write.csv2(nombre,"nombre.csv",quote=F,row.names=T)
# en Excel > abrir 
# completar celda A1
write.table(nombre,"nombre.txt",quote=F,dec=",",row.names=F)
# dec : separador decimal , (versus .)
# en Excel > abrir > asistente para importar > delimitados > check tabulación y espacios
# para una sola variable
write.table(nombre,"nombre.txt",quote=F,dec=",",row.names=T)
# dec : separador decimal , (versus .)
# en Excel > abrir > asistente para importar > delimitados > check tabulación y espacios
# en Excel correr la fila desde la celda A1 para alinear las columnas 
# para mas de una variable
library(WriteXLS)
>WriteXLS(miobjeto, ExcelFileName ="miarchivo.xls")
#

# 
# leer URL
# carta de colores en R
uri<-"http://www.stat.columbia.edu/~tzheng/files/Rcolor.pdf"
download.file(uri,"coloresR.pdf",method="internal",mode="wb")

#
# guardar los parámetros gráficos iniciales para modificarlos y volver al par base
# ver parámetros gráficos actuales
par()              
# hacer una copia de parámetros gráficos actuales
cpar<-par()     
# cambiar par, por ejemplo para un gráfico de poster o ppt
par(bg="#62629B", cex.axis=2, cex.lab=2.3,
    col="white",  col.axis="white", col.lab="white",
    fg="white",mar=c(5.1,5.1,3.5,1.5))
# crear un gráfico con los nuevos parámetros
hist(data$x)
# restaurar (volver) a parámetros gráficos iniciales
par(cpar)
